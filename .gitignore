node_modules/
dist/
*.log
npm-debug.log*

.env
.env.local
*.local.*

# The semantic index lives outside the vault, but guard against it ever ending up in-repo.
*.sqlite
*.sqlite-shm
*.sqlite-wal

# @xenova/transformers' local model cache, if ever created under the project dir.
.cache/

# tesseract.js OCR language data, if ever created under the project dir (should live in
# ~/.second-brain-mcp/ via the explicit cachePath in ocr.ts, but guard against regressions).
*.traineddata
